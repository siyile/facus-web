{
  "name": "infrastructure",
  "version": "1.0.0",
  "main": "dist/server.js",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.8",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "tsc && node dist/server.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 0",
    "clean": "rm -rf dist"
  }
}
